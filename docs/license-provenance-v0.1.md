# License and upstream provenance

CMCP-TIME is the public product name. The compatible package and plugin identifier is `cmcp`. This source candidate is separate from an npm registry release. A Git tag identifies a published snapshot only after the maintainer creates it.

Current project destination: [redwakame/CMCP-TIME](https://github.com/redwakame/CMCP-TIME). The older repository below is retained as upstream provenance, not as the new distribution destination.

The current distribution and project-authored additions use [Apache-2.0](../LICENSE), except where a file states otherwise. Original author and upstream: **redwakame**, [redwakame/cmcp](https://github.com/redwakame/cmcp). Keep [NOTICE](../NOTICE) with redistributed material.

The upstream baseline `2bdb94d0fcd12ae85009f7a52ecf67617cab49bb` declared MIT-0. [LICENSES/MIT-0.txt](../LICENSES/MIT-0.txt) and the [76-path baseline index](../LICENSES/upstream-MIT-0-paths.json) preserve that origin, original Git blob identifiers and hashes. That index describes upstream material, not the contents of this candidate. Current package hashes are in [PUBLIC-FILES.json](../PUBLIC-FILES.json).

Apache-2.0 does not revoke permissions already granted for upstream versions. Inherited files can contain MIT-0 baseline material and Apache-2.0 modifications. Older names in NOTICE identify provenance, not a mandatory Host dependency. No per-use advertising, mandatory promotion or endorsement requirement is added.

Node.js, PowerShell, Codex and model services are separate dependencies under their own terms; no model weights or npm dependency tree are bundled. Runtime conversations, user data, credentials, local grants, development sessions and private verification records are excluded and are not relicensed by this distribution.

This packaging revision adds distribution metadata and operating documentation and adapts synthetic tests. It preserves Runtime source and existing licenses. See [release notes](../RELEASE-NOTES.md) for scope and verification limits. Provenance was checked against the local upstream baseline; it is not a claim that the remote has no newer commits.
